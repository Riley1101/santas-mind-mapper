import { Background } from "reactflow";
/**
 * SantaBackground Component for canvas
 *
 * A customized background component for the SantaFlow visualization using ReactFlow.
 * You can customize more here
 */
export function SantaBackground() {
  return <Background gap={12} size={1} />;
}
