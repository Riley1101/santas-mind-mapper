import { MiniMap } from "reactflow";

/**
 * SantaMiniMap Component
 *
 * A mini map component for the SantaFlow visualization using ReactFlow.
 */
export function SantaMiniMap() {
  return <MiniMap position="bottom-right" />;
}
